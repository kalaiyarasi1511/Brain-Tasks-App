version: 0.2

phases:
  install:
    commands:
      - echo "Using prebuilt dist; no install required"
  pre_build:
    commands:
      - echo "Pre-build: nothing"
  build:
    commands:
      - echo "Build: skipping - packaging repository files"

artifacts:
  files:
    - appspec.yml
    - scripts/**/*
    - dist/**/*
    - Dockerfile*
    - buildspec.yml
  discard-paths: no
